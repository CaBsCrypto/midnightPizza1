# Frontend (Vite) — variables públicas, se incluyen en el bundle del navegador.
VITE_PRIVY_APP_ID=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Backend (funciones serverless en /api, configurar en el dashboard de Vercel).
# NUNCA exponer SUPABASE_SERVICE_ROLE_KEY con el prefijo VITE_ ni en código de cliente.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
